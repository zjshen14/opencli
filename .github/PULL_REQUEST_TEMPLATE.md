<!--
Thanks for contributing to OpenCLI! Before opening this PR, please make sure:
- Your branch is rebased on latest `main`
- All checks pass locally: `npm run typecheck && npm run lint && npm run format:check && npm test`
- The change is linked to a GitHub issue (use `Closes #<number>` or `Part of #<number>` below)

See [docs/task-workflow.md](../docs/task-workflow.md) for the full task workflow.
-->

## Summary

<!-- 1-3 bullet points: what changed and *why* (not just what). Link to the issue if not already auto-linked below. -->

-

## Related issue

<!-- Use `Closes #<number>` if this PR fully resolves the issue, or `Part of #<number>` / `References #<number>` if partial. -->

Closes #

## Test plan

- [ ] `npm run typecheck && npm run lint && npm run format:check && npm test` — all pass
- [ ] <!-- manual testing steps if UI/behavior changes -->
- [ ] <!-- edge cases or regressions to check -->

## Notes for reviewers

<!-- Optional. Call out decisions, trade-offs, follow-up work, or anything reviewers should pay extra attention to. Delete this section if not needed. -->
